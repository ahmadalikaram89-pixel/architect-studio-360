# قائمة المهام القادمة

كل النقاط هون معروفة ومقصودة — إما حكينا عنها صراحة وأجّلناها لمرحلة لاحقة، أو من أفكار "الخطوة الجاية" الأصلية للمشروع. مرتبة حسب الأولوية تقريباً.

## منجز
- [x] تصدير المخطط 2D كملف PDF / طباعة (زر "طباعة / PDF" بالهيدر — كل الطوابق، كل طابق بصفحة لحاله)
- [x] توحيد الجدران المشتركة بين غرفتين متلاصقتين تماماً (جدار واحد بدل جدارين مضاعفين، بلا نوافذ عليه)
- [x] حذف طابق أو تبديل ترتيب طابقين متجاورين
- [x] قائمة لتصفح كل مشاريعك السابقة والتبديل بينها
- [x] نسيت كلمة السر (إعادة تعيين عبر إيميل) — رابط "نسيت كلمة السر؟" بشاشة الدخول + شاشة تعيين كلمة سر جديدة بعد الرجوع من رابط الإيميل
- [x] توحيد جدران غرف متلاصقة بأطوال مختلفة (تلامس جزئي) — الجزء المتراكب فعلياً بس ياخد جدار مدموج واحد، والباقي من كل غرفة يبني عادي بحاله
- [x] حذف أو تحريك باب/نافذة بعد إضافتها — نقر على علامة الباب/النافذة بالمخطط يحددها ويظهر شريط أدوات (نقل/حذف)
- [x] إصلاح: زر إضافة باب/نافذة كان يطفي وضع الإضافة تلقائياً بعد أول عنصر
- [x] سطح الغرفة يدوي مو تلقائي (زر "سطح الغرفة" بعرض 3D) + خيار "شرفة" بقائمة تسمية الغرف — الغرفة بلا سطح تظهر مكشوفة، مناسبة لشرفة/تراس
- [x] أثاث ثلاثي الأبعاد جاهز يُضاف للغرف — كتالوج 5 قطع (سرير، أريكة، طاولة، خزانة، مغسلة)، إضافة/نقل/تدوير/حذف بنفس نمط الأبواب والنوافذ
- [x] شعار البرنامج الرسمي بدل أيقونة Box العامة (بكل شاشات الهيدر + أيقونة تبويب المتصفح)
- [x] ارتفاع أو لون جدران مختلف لكل طابق — عناصر التحكم بالشريط الجانبي صارت خاصة بالطابق المعروض حالياً، بلا أي تأثير على الطوابق التانية أو المشاريع الموجودة أصلاً
- [x] سلالم — عنصر ربط بصري بين الطوابق — سلم إجرائي يوصل بالضبط لسطح الطابق التالي (يتكيّف تلقائياً مع ارتفاع كل طابق)، إضافة/نقل/تدوير/حذف بأي مكان على الشبكة
- [x] قياسات أدق وجدران مائلة/غير مستطيلة — مفتاح "الصق بالشبكة" + لوحة "قياسات دقيقة" رقمية للغرف المستطيلة، وأداة "ارسم شكل حر" لغرف بجدران مايلة (مضلع حر بأي عدد أضلاع)؛ الإصدار الأول للغرف الحرة جدرانها صلبة بس (بلا أبواب/نوافذ/أثاث/دمج جدران مشتركة عليها — موثّق كقيد واضح)
- [x] تفاصيل نتوءات دقيقة على قوام الجدران (normal map إجرائي — نتوءات ناعمة تبين بس عند تكبير قريب، بلا أي تأثير على الأداء أو الألوان)
- [x] إصلاح: بلاطة السقف كانت تظهر دايماً حتى بلا الضغط على "إضافة سطح"، وإصلاح متابع لحالة كانت البلاطة تظهر بأي غرفة بمشروع متعدد الطوابق حتى بلا تراكب فعلي
- [x] إصلاح: شريط أدوات السلم كان يختفي عند الحافة العليا للشبكة + معالجة احتمال تعطّل عرض 3D عند تصغير نافذة المتصفح
- [x] خطوط قياس (dimensions) بأسلوب CAD حوالين كل غرفة مستطيلة بالمخطط 2D وبالطباعة — زر تفعيل/تعطيل مستقل بالمخطط الحي، دايماً ظاهرة بنسخة الطباعة
- [x] قوس اتجاه فتح الباب بالمخطط 2D (يحسب اتجاهه ديناميكياً نحو داخل الغرفة بدل جدول ثابت)، رمز سلم أوضح (خطوط الدرجات بدل مستطيل بسيط)، وأنواع غرف جديدة ("جلوس"، "مضافة")
- [x] أبواب ونوافذ وأثاث على الغرف الحرة (الشكل غير المستطيل) — عمود `openings.edge_index` جديد (بدل `wall` المقيّد بأربع جهات فقط)، وجدران الأضلاع المايلة صارت تبني أعمدة/فتحات حوالين الأبواب والنوافذ (نفس منطق الجدران المستقيمة)، واختبار "نقطة داخل مضلع" لاحتواء الأثاث. خارج النطاق لسا (متروك عن قصد): دمج جدران مشتركة بين غرفة حرة وجارتها — أصعب بكتير هندسياً، كل غرفة حرة تبني جدرانها لحالها دايماً حتى لو لامست غرفة تانية
- [x] تحديث شامل لثيم ألوان البرنامج — مقياس رمادي محايد (بلا صبغة زرقاء) بدل slate الافتراضي بكل الواجهة، وخلفية بيضاء بدل السوداء بالمخطط 2D والعرض 3D وشاشات التطبيق كافة (أقرب لهوية برنامج تصميم هندسي احترافي متل AutoCAD/SketchUp)
- [x] تراجع/إعادة (Undo/Redo) على كل تعديلات التصميم — غرف (رسم/قياسات دقيقة/تسمية/حذف/سطح)، أبواب ونوافذ، أثاث، سلالم، ولون/ارتفاع جدران الطابق. زران بالهيدر + اختصار Ctrl+Z / Ctrl+Shift+Z. يعتمد على لقطات كاملة للتصميم (rooms/openings/furniture/stairs) قبل كل عملية، والتراجع/الإعادة بيحسبوا الفرق مع قاعدة البيانات (إدراج/تحديث/حذف) ويطبّقوه فوراً — نفس جداول العمليات العادية تماماً، بلا مسار خاص أو خطر عدم تزامن
- [x] معالجة أخطاء المستخدم — نظام إشعارات (toast) بأسفل الشاشة بيظهر فوراً لما يفشل حفظ أي تعديل (غرف/فتحات/أثاث/سلالم/مراحل...)، بدل ما كان الفشل يُسجَّل بالكونسول بصمت وبلا أي علم للمستخدم (~30 موضع). وإصلاح خطأ حقيقي: تبديل مشروع (أو التحميل الأولي) كان ممكن يعلَّق شاشة التحميل للأبد لو فشل الاتصال أثناء جلب بيانات المشروع، بلا رسالة ولا طريقة للتعافي إلا بإعادة تحميل الصفحة
- [x] تقسيم `ArchitectStudio.jsx` (كان فوق 2700 سطر) لملفات أصغر — `lib/planGeometry.js` (دوال هندسية/رسم/التقاط بلا React)، `components/Viewport3D.jsx`، `components/ProjectSetup.jsx`، `components/PhaseTracker.jsx` (مع `PhaseCard`)، `components/PreciseMeasurePanel.jsx`. الملف الرئيسي صار ~1650 سطر (بدل 2700+)، بلا أي استيراد غير مستخدم، وبلا تغيير بالسلوك — فحص شامل بمتصفح حقيقي غطّى كل الشاشات (الإعداد، المراحل، المخطط 2D بكل عناصره، القياسات الدقيقة، الشكل الحر، التراجع/الإعادة، العرض 3D، الطباعة) قبل الدمج
- [x] توسيع مكتبة الأثاث لـ 18 قطعة احترافية (بدل 5) مبوّبة حسب نوع الفراغ — حمام (مرحاض/بانيو/كابينة دش/مغسلة)، مطبخ (كاونتر/موقد/ثلاجة)، صالة/سفرة (أريكة/كرسي بذراعين/وحدة تلفزيون/طاولة سفرة)، مكتب (مكتب/مكتبة)، غرفة نوم (سرير/خزانة/كومودينو) — كل هاي العناصر ضرورية لأي مخطط معماري احترافي بيراجعه مهندس، مو بس أثاث زخرفي
- [x] سطح جملوني (gable) — خيار "نوع السطح: مسطح / جملوني" لأي غرفة مستطيلة (بأعلى طابق بلا طابق فوقها) عندها سطح مفعّل. حافة (ridge) بمحاذاة البعد الأطول، ميل 28° عبر البعد الأقصر، وجدار مثلثي بلون الجدران على كل طرف. خارج النطاق (موثّق): الغرف الحرة الشكل (points) — سطح مسطح بس، نفس قيود الأبواب/النوافذ عليها أصلاً
- [x] تصدير DXF (زر "تصدير DXF" بالهيدر) — مخطط 2D حقيقي بصيغة CAD (جدران مزدوجة الخط مقسّمة حوالين الفتحات، قوس اتجاه فتح الباب كعنصر ARC، علامات نوافذ، خطوط قياس، تسميات الغرف والمساحات، السلالم)، مبوّب بطبقات (WALLS/DOORS/WINDOWS/DIMENSIONS/TEXT/STAIRS) وكل الطوابق بملف واحد جنب بعض. **ملاحظة مهمة**: هاد DXF مو DWG الثنائي الحقيقي — تنسيق DWG مُغلق ومملوك لشركة Autodesk وبيحتاج SDK مُرخّص مش ممكن تضمينه بتطبيق ويب؛ DXF هو التنسيق المفتوح البديل يلي AutoCAD وكل برامج CAD تقريباً بتفتحه مباشرة بلا أي تحويل — نفس النتيجة العملية (ملف يفتح على AutoCAD ويُسلَّم لمقاول/دائرة ترخيص) بأداة قابلة للتنفيذ فعلياً. تصدير IFC (نموذج BIM ثلاثي الأبعاد) ضل خارج النطاق — أصعب بكتير من مخطط 2D بسيط
- [x] مكتبة مواد/خامات حقيقية — 6 مواد procedural (طلاء ناعم، طوب بمداميك حقيقية، بلاط بخطوط جص، خشب بألياف وألواح، رخام بعروق، خرسانة مكشوفة خشنة)، كل وحدة برسم نقش مختلف فوق قماش (canvas) وقت التشغيل بدل الضجيج العشوائي الموحّد القديم (بلا أي صورة أو ملف خارجي، نفس فلسفة كل هندسة البرنامج). مادة الجدران قابلة للتحديد لكل طابق (نفس نمط لون/ارتفاع الجدران — bulk بزر واحد)، ومادة أرضية كل غرفة قابلة للتحديد لحالها (بلوحة جديدة بعرض 3D بتسمح كمان بتغيير لون أرضية غرفة موجودة أصلاً — ميزة ما كانت متوفرة قبل هلق حتى للون العادي)
- [x] سطح "أربعة ميول" (hip) — خيار ثالث بقائمة نوع السطح (مسطح/جملوني/أربعة ميول) لأي غرفة مستطيلة بأعلى طابق. هندسة مستوية حقيقية بأربعة أوجه (وجهان شبه منحرف على الضلعين الطويلين + وجهان مثلث على الضلعين القصيرين، كلهم بميل ROOF_PITCH نفسه)، مبنية مباشرة كـ BufferGeometry (بلا تقريب صناديق) — طول الحافة = البعد الطويل ناقص القصير، فبيتحول تلقائياً لسطح هرمي بأربعة أوجه لغرفة مربعة (حالة خاصة بلا كود إضافي). نفس قيود الجملوني: الغرف الحرة الشكل (points) سطح مسطح بس
- [x] بنية اختبار آلي أساسية — `vitest` (بيئة jsdom) + سكربت `npm test`، مع GitHub Actions (`.github/workflows/test.yml`) بيشغّله تلقائياً على كل push/PR لـ main. اختبارات وحدة للدوال الهندسية الصرفة بـ `lib/planGeometry.js`/`lib/build3d.js` (snap/clamp/pointInPolygon/roomArea/computeSharedBoundaries...)، **والأهم**: اختبار انحدار مباشر (`lib/__tests__/schema-consistency.test.js`) بيقرأ `supabase-schema.sql` كنص خام ويقارن قوائم قيود CHECK فيه (furniture.kind, rooms/projects.wall_material, rooms.floor_material, rooms.roof_type) بكتالوجات JS المصدر (`FURNITURE_KINDS`, `MATERIALS`) — نفس صنف الباگ يلي صار فعلياً بـ PR #37 (furniture_kind_check ما كان محدَّث)، اتأكد بتجربة مباشرة إنه هاد الاختبار كان رح يمسكه فوراً محلياً/بـ CI قبل ما يوصل للمستخدم
- [x] جدول كميات (BOQ) تقديري أولي — زر "جدول الكميات" بالهيدر بيفتح لوحة بتحسب تلقائياً من التصميم الحالي: مساحة البناء، طول الجدران الصافي (بلا تكرار الجدار المشترك بين غرفتين متلاصقتين — إعادة استخدام مباشرة لـ `computeSharedBoundaries` الموجودة أصلاً)، مساحة الأسطح المغطاة، عدد الأبواب/النوافذ/السلالم/الغرف/الطوابق. سعر الوحدة لكل بند قابل للتعديل يدوياً (حالة محلية بالمتصفح، مو جزء من بيانات المشروع — الأسعار بتفرق كتير حسب السوق المحلي والوقت) والإجمالي بيتحدث فوراً، وزر "تصدير CSV" للجدول كامل. موثّق بوضوح بالواجهة إنه تقدير أولي بس، مو جدول كميات هندسي نهائي بديل عن مكتب حساب كميات

## بعيد المدى
- [ ] تصدير IFC (نموذج BIM ثلاثي الأبعاد كامل) — مشروع أكبر بكتير من تصدير DXF 2D المُنجز، يحتاج تمثيل هندسي ثلاثي الأبعاد كامل لكل عنصر (جدران/أسقف/فتحات) بمعايير BIM
